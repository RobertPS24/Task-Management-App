<Window x:Class="TaskManagementApp.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="350" Width="400" WindowStartupLocation="CenterScreen">
    <StackPanel Margin="20">
        <TextBlock Text="Welcome" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

        <StackPanel Margin="0,0,0,10">
            <Label Content="Username"/>
            <TextBox x:Name="UsernameBox" FontSize="14"/>
        </StackPanel>

        <StackPanel Margin="0,0,0,10">
            <Label Content="Password"/>
            <PasswordBox x:Name="PasswordBox" FontSize="14"/>
        </StackPanel>

        <Button Content="Login" Click="Login_Click" Height="35"/>
        <Button Content="Register" Click="GoToRegister_Click" Margin="0,10,0,0" Height="30"/>
    </StackPanel>
</Window>
